# Backend

## Setup

* `cd /api && npm i` install node modules for api directory.
* `cd /functions && npm i` install node modules for functions directory.
* `npm run seed` to fill your local firestore with test data.

## Development

* `npm run start` Start firebase emulators locally for development.

## Testing

* `npm run test` Will start mocha tests in watch mode.
